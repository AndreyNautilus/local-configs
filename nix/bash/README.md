# bash config files

## Prompt - PS1 envvar

`.bashrc-ps1` contains git-aware prompt inspired by
[bobthefish](https://github.com/oh-my-fish/theme-bobthefish) fish theme.

1. copy `.bashrc-ps1` into `$HOME`;
2. call it from the standard `.bashrc`:
    ```bash
    . ~/.bashrc-ps1
    ```
